import 'dart:ui';

const colorAppBar = Color.fromRGBO(209, 40, 5, 1);
const colorEnterCode = Color.fromRGBO(0, 0, 0, 0.5);
const colorBackgr = Color(0x00000080);
const colorButton = Color.fromRGBO(241, 44, 44, 1);
